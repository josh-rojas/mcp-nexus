# MCP Nexus Environment Configuration
# Copy this file to .env.local for local development overrides

# =============================================================================
# DEVELOPMENT VARIABLES
# =============================================================================

# Enable Tauri debug logging (default: false)
# Accepted values: true, false, 1, 0, yes, no
# VITE_DEBUG_TAURI=false

# API request timeout in milliseconds (default: 30000)
# Range: 1000-120000 (1 second to 2 minutes)
# VITE_API_TIMEOUT=30000

# PulseMCP Marketplace API base URL (default: https://registry.pulsemcp.com)
# VITE_MARKETPLACE_URL=https://registry.pulsemcp.com

# Enable React Query DevTools in development (default: true in dev, false in prod)
# VITE_ENABLE_DEVTOOLS=true

# =============================================================================
# BUILD VARIABLES
# =============================================================================

# Application version for display (default: from package.json)
# VITE_APP_VERSION=0.1.0

# Build timestamp (default: current date/time)
# VITE_BUILD_DATE=2024-01-08T00:00:00.000Z

# Sentry DSN for error reporting (optional, not yet implemented)
# VITE_SENTRY_DSN=

# =============================================================================
# NOTES
# =============================================================================

# Environment variables are validated at application startup
# Invalid values will fallback to defaults with console warnings
# All variables are optional and have sensible defaults
# Never commit .env.local or files containing sensitive values
# See docs/environment-setup.md for detailed configuration guide
